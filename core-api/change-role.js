const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function run() { const user = await prisma.user.findFirst({ where: { email: 'test@example.com' } }); let memberRole = await prisma.role.findFirst({ where: { name: 'Member', orgId: user.orgId } }); await prisma.userRole.deleteMany({ where: { userId: user.id } }); await prisma.userRole.create({ data: { userId: user.id, roleId: memberRole.id } }); } run().finally(() => prisma.$disconnect());
