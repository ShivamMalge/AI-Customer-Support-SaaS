<USER_REQUEST>
# SupportGPT — Phase 2 Implementation Prompt (Software Architecture Scaffolding)

## ROLE

You are a senior backend/infra engineer implementing the **architecture scaffolding only** for a project
called SupportGPT. You are NOT implementing business logic, AI features, or UI polish in this phase.
This is Phase 2 of an 11-phase plan: **Software Architecture**. Your job is to stand up the skeleton —
two backend services, the data layer, and local orchestration — exactly as specified below, with nothing
invented and nothing skipped.

---

## HARD RULES (violating any of these is a failure, even if the code "runs")

1. **Do not invent folder names, service names, or structure.** Use exactly the structure given in
   "TARGET FOLDER STRUCTURE" below. If you think a different structure is better, stop and ask — do not
   silently change it.
2. **Do not hardcode any of the following, anywhere, ever**: database URLs, ports, API keys, secrets,
   provider URLs, org IDs, credit limits, file size limits. Every one of these must come from an
   environment variable, loaded and validated through a typed config module (see "CONFIG VALIDATION").
   If you catch yourself typing a literal value that looks like a URL, port, secret, or limit, stop and
   move it to config instead.
3. **Two backend services, never merged.** `core-api` (Node.js + NestJS + TypeScript) and `ai-service`
   (Python + FastAPI) are separate deployable services with separate `package.json`/`pyproject.toml`,
   separate Dockerfiles, and separate CI workflows. Do not put AI/RAG code in `core-api`. Do not put
   auth/ticketing code in `ai-service`.
4. **No business logic yet.** Do not implement ticket creation, chat, RAG, or billing logic in this
   phase. Endpoints/modules should exist as empty or stubbed handlers that return a `501 Not Implemented`
   or a placeholder response. The goal is a working skeleton that boots, connects to its database, and
   passes health checks — nothing more.
5. **Every servi
<truncated 8534 bytes>
re-api` → `ai-service`)
Implement `src/clients/ai-service.client.ts` in `core-api` as a thin HTTP client with:
- Base URL read from config (`AI_SERVICE_URL`), never hardcoded.
- An explicit timeout (read from config, e.g. `AI_SERVICE_TIMEOUT_MS`, with a sane non-secret default
  you may hardcode as a fallback — state this explicitly in a comment).
- One method: `checkHealth()` that calls `ai-service`'s `/health` and returns a boolean.

**STOP AND VERIFY:** Write and run one integration test that calls `checkHealth()` against the running
`ai-service` container and asserts it returns `true`.

---

### Step 6 — CI skeleton
Create `.github/workflows/core-api.yml` and `.github/workflows/ai-service.yml`, each triggered only on
changes to their respective folder (use `paths:` filters), running install + lint + build/test for that
service only. Do not create a single combined workflow.

**STOP AND VERIFY:** Explain (in your response, not in code) how you tested that a change to
`core-api/` does not trigger `ai-service.yml` and vice versa — e.g. by describing the `paths:` filter
you used and why it's correct. If you cannot actually run GitHub Actions locally, say so explicitly
rather than claiming you verified it.

---

## AFTER ALL STEPS: FINAL SELF-CHECK

Before declaring Phase 2 complete, go back through the HARD RULES list at the top and confirm, one by
one, that none were violated. Explicitly list each rule and state "confirmed" or flag where you deviated
and why. Do not skip this — a silent violation found later is much more expensive to fix than one you
flag now.

## OUT OF SCOPE FOR THIS PROMPT (do not build these yet)
- Full Prisma/SQLAlchemy schema (only `organizations`/`users` for now)
- Any actual authentication logic
- ClickHouse, BullMQ job processing, OpenTelemetry
- Kubernetes manifests (Docker Compose only, for local dev)
- Frontend implementation
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-07-13T08:49:37+05:30.
</ADDITIONAL_METADATA>

---

